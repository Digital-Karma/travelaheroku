{% extends "base.html.twig" %}

{% block title %}
    Edition Focus Ville
{% endblock %}

{% form_theme form 'focus_ville/_collection.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Modifier le focus ville: {{focus_ville.title}}</h1>

        {{ form_start(form) }}

        {{form_widget(form)}}
    
        <button type="submit" class="btn btn-primary">Valider Modification</button>

        {{ form_end(form) }}
    </div>   
{% endblock %}
